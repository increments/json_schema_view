Description:
  Generates the configuration file (config/initializers/json_schema_component.rb) and setup components dir (app/components).:

Example:
  `bin/rails generate json_schema_component:install`
